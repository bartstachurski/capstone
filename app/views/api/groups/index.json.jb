@groups.map do |group|
  {
    name: group.name,
    id: group.id,
    user_id: group.user_id,
    created_at: group.created_at,
    saved_breweries: group.saved_breweries,
    saved_brewery_groups: group.saved_brewery_groups,
    photo_url: group.photo_url
  }
end